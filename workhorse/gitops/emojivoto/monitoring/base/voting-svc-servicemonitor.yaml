---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: voting-svc
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: voting-svc
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
